ChangMin.updateBranding(function (brand) {
    return brand.set('name', 'Coin');
});

ChangMin.updateMenu(function (menu) {
    return menu
        .set('favorite', ChangMin.createMap({
            label: 'Favorite',
            children: {
                dashboard: {
                    icon: 'fa fa-home',
                    label: 'Dashboard',
                    link: '{{ path('changmin_admin_dashboard') }}',
                },
                web_user: {
                    icon: 'fa fa-user',
                    label: 'Web Users',
                    link: '{{ path('sylius_admin_web_user_index') }}',
                }
            }
        }))
        .set('torocoin', ChangMin.createMap({
            label: 'Coins',
            children: {
                balance: {
                    icon: 'fa fa-bitcoin',
                    label: 'Balance',
                    link: '{{ path('toro_coin_admin_balance_index') }}',
                },
                transaction: {
                    icon: 'fa fa-exchange',
                    label: 'Transactions',
                    link: '{{ path('toro_coin_admin_transaction_index') }}',
                },
                topup: {
                    icon: 'fa fa-heart',
                    label: 'Topup',
                    link: '{{ path('toro_coin_admin_transaction_topup_index') }}',
                },
            }
        }))
    ;
});
